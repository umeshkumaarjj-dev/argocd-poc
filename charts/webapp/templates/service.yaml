apiVersion: v1
kind: Service
metadata:
  name: {{ include "webapp.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "webapp.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
